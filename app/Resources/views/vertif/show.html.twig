{% extends 'base.html.twig' %}

{% block body %}
    <h1>Vertif</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vertif.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ vertif.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('vertif_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('vertif_edit', { 'id': vertif.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
