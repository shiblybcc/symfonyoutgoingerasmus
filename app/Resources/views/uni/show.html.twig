{% extends 'base.html.twig' %}

{% block body %}
    <h1>Uni</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ uni.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ uni.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('uni_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('uni_edit', { 'id': uni.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
