{% extends 'base.html.twig' %}

{% block body %}
    <h1>Stdn</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stdn.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ stdn.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('stdn_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('stdn_edit', { 'id': stdn.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
